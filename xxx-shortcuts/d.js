/*
const { execute: addCmd } = require("./add-command")
const { execute: lsCmd } = require("./ls-command")

async function execute(commandArgs, cmmandOpts, deps) {
    console.log("demo is starting")
    console.log("\nADD ------------------------")

    await addCmd(["planning", "2018-06-21"], {}, deps)
    await addCmd(["Failed again -> post-mortem", "today"], {}, deps)
    await addCmd(["kill the runner", "yesterday"], {}, deps)

    console.log("\nLIST -----------------------")

    lsCmd([], {}, deps)
}

module.exports = { execute }
*/
